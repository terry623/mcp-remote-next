{
  "name": "mcp-remote-next-tests",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "pnpm build:parent && vitest run",
    "test:watch": "pnpm build:parent && vitest watch",
    "build:parent": "cd .. && pnpm build"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.23.0",
    "@types/node": "^22.10.2",
    "vitest": "^2.1.8"
  }
}
